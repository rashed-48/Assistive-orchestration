[pytest]
testpaths = tests/automated
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    model: loads a sentence-transformer / Whisper model (slow; excluded by default)
addopts = -ra -m "not model"
